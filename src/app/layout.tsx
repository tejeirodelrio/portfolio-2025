import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Manuel Tejeiro Del Río | Salesforce Architect",
  description: "Senior Salesforce Solution Architect specializing in Field Service, Nonprofit Cloud, and Education Cloud. Expert in enterprise architecture, digital transformation, and platform integration.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased text-lg md:text-xl`}
      >
        {children}
      </body>
    </html>
  );
}
